# Claude Pi-Chart Cockpit Design Drop

Drop the Claude design export for `Pi-Chart Cockpit.html` here.

Preferred contents:

- `Pi-Chart Cockpit.html`
- Any generated `README.md` from Claude's export
- Any local assets folder from the export
- The original zip, only if it is small enough to keep as review context

This folder is a staging inbox for prototype input. It is not canonical
product documentation. After implementation, durable design decisions should
move into tracked project docs such as `README.md`, `ARCHITECTURE.md`,
`DESIGN.md`, or a future UI design document.
