# Context snapshot — next phase of ADR 018

Task: `$ralplan next phase of ADR 018`.

Desired outcome: consensus plan for the next ADR 018 phase after completed docs/source-authority and QBN-001 banner execution. Planning only; do not implement product/source changes.

Known facts/evidence:
- ADR 018 is accepted and sets `pi-chart` identity as clinical truth/provenance substrate, not cockpit UI (`decisions/018-architecture-rebase-clinical-truth-substrate.md:26-37`).
- ADR 018 chose hybrid migration now and keeps clean-slate service/event-store as serious candidate gated by spike + ADR 019 (`decisions/018-architecture-rebase-clinical-truth-substrate.md:30-33`, `:56-70`, `:93-100`).
- Source-authority map says next architecture gate is ADR 019 after clean-slate service/event-store spike comparing against patient_002 projections (`docs/architecture/source-authority.md:123-125`).
- Board says ARCH-001 docs/source-authority lane complete and next action is deciding clean-slate spike scope before product-code refactor (`docs/plans/kanban-prd-board.md:66`).
- Current core thesis: chart canonical; current state query; derived summaries disposable (`README.md:11-17`).
- Current public read surface is six view primitives (`README.md:122-138`; `DESIGN.md:354-369`).
- Current write/read paths are filesystem-backed via events.ndjson/notes/vitals files (`ARCHITECTURE.md:142-179`).
- Current storage is intentionally not sacred production storage per ADR 018 (`decisions/018-architecture-rebase-clinical-truth-substrate.md:33`).
- Current views/loaders directly walk filesystem (`src/views/active.ts:37-55`, `src/read.ts:41-189`, `src/validate.ts:1076-1211`).
- Current write path validates and appends concrete filesystem files (`src/write.ts:392-451`).
- Existing tests are strong but lack fuzz/property/perf (`ARCHITECTURE.md:235-246`).
- QBN-001 applied banners to six tracked historical/prototype docs; unrelated pre-existing dirty files remain.

Constraints:
- Planning only in this ralplan turn.
- No `src/`, `schemas/`, `patients/`, `scripts/`, generated, canonical ADR/source-authority edits unless a later execution plan authorizes them.
- Preserve hidden pi-sim boundary and pi-agent bounded-context boundary.
- Do not let UI prototype or patient_002 overfit become architecture.
- Plan should be executor-safe for AI agents; minimize context poison.

Unknowns/open questions:
- Whether the human wants Option B clean-slate to become default if spike succeeds or only a candidate.
- Exact service/store technology remains undecided and should not be chosen prematurely.
- Whether spike should be docs/test-harness-only or include source prototype under separate sandbox path.

Likely touchpoints for later execution:
- New plan/PRD/test-spec for ADR 019 clean-slate spike.
- Possible tracked docs: `docs/plans/prd-adr018-next-phase-clean-slate-spike.md`, `docs/plans/test-spec-adr018-next-phase-clean-slate-spike.md`, `.omx/plans/plan-adr018-next-phase-clean-slate-spike.md`.
- Later spike may touch isolated experimental adapter/prototype files, but not in this planning turn.
