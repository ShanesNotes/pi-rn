# Context snapshot — V03-S4 profiles + ADR17-17a actor classification

## Task statement
Plan execution from `docs/plans/handoff-v03-s4-and-adr17-17a-parallel.md` using `$ralplan` consensus mode.

## Desired outcome
Produce an execution-ready plan for two disjoint implementation lanes:
- Lane 1: V03-S4 profile envelope/registry/validator foundation.
- Lane 2: ADR17-17a projection-only authorship classification.

## Known facts / evidence
- Handoff file exists at `docs/plans/handoff-v03-s4-and-adr17-17a-parallel.md`.
- Lane 1 owned files: `schemas/event.schema.json`, `schemas/profiles/index.json`, `src/validate.ts`, `src/validate.test.ts`.
- Lane 2 owned files: `src/views/projection.ts`, `src/views/projection.test.ts`.
- Brownfield check shows no current `profile` schema field, no `V-PROFILE` validation, and no `schemas/profiles/` directory.
- Brownfield check shows `src/views/` exists with no `projection.ts`/`AuthorshipClass` helper.
- `package.json` scripts: `npm test`, `npm run typecheck`, `npm run check`.

## Constraints
- No shared write targets between lanes.
- Lane 2 must not read `event.profile`.
- No edits under `decisions/` or `docs/plans/` during execution.
- No new review/attestation top-level event fields.
- No `pi-agent` or `pi-sim` coupling.
- No dependency or lockfile changes.

## Unknowns / open questions
- Exact existing test helper names in `src/validate.test.ts` must be confirmed before Lane 1 edits.
- Existing import/type conventions in `src/views/*.ts` should be followed before creating `projection.ts`.

## Likely codebase touchpoints
- Lane 1: `schemas/event.schema.json`, `schemas/profiles/index.json`, `src/validate.ts`, `src/validate.test.ts`.
- Lane 2: `src/views/projection.ts`, `src/views/projection.test.ts`.
