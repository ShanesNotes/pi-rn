{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pi-rn.local/schemas/vitals.schema.json",
  "title": "pi-chart vitals row",
  "description": "One sample in timeline/<day>/vitals.jsonl. Carries the same provenance shape as events so monitor outputs are traceable to their source extension and addressable via the vitals:// evidence URI.",
  "type": "object",
  "required": ["sampled_at", "subject", "encounter_id", "source", "name", "value"],
  "properties": {
    "sampled_at": {
      "type": "string",
      "format": "date-time",
      "description": "Wall-clock or sim-clock instant the value was observed."
    },
    "recorded_at": {
      "type": "string",
      "format": "date-time",
      "description": "Chart-ingest/write time. Does not replace sampled_at for clinical windows."
    },
    "sample_key": {
      "type": "string",
      "pattern": "^vital_[0-9a-f]{16}(?:_[0-9]+)?$",
      "description": "Deterministic stream-row identity key for correction/refutation without event-izing every sample."
    },
    "subject":      { "type": "string", "description": "Must equal chart.yaml.subject." },
    "encounter_id": { "type": "string", "description": "Encounter the sample belongs to. Required so vitals:// URIs can scope by encounter." },
    "source": {
      "type": "object",
      "required": ["kind"],
      "properties": {
        "kind": {
          "type": "string",
          "description": "Provenance channel, e.g. monitor_extension, lab_analyzer, poc_device."
        },
        "ref": {
          "type": "string",
          "description": "Source-specific identifier, e.g. pi-sim-monitor or instrument serial."
        }
      },
      "additionalProperties": false
    },
    "name":  { "type": "string", "description": "Metric name, snake_case (heart_rate, spo2, bp_systolic, ...)." },
    "value": {
      "type": ["number", "string", "boolean"],
      "description": "Numeric for most metrics; strings/bools allowed for categorical readings."
    },
    "unit":  { "type": "string" },
    "context": {
      "type": "object",
      "description": "Modifiers that condition the reading, e.g. o2_device, o2_flow_lpm, position."
    },
    "quality":  {
      "description": "Signal quality. Legacy scalar values remain accepted during migration; canonical form is {state, flags?}. currentState drops invalid samples; questionable samples remain visible to downstream readers.",
      "oneOf": [
        { "enum": ["valid", "questionable", "invalid"] },
        {
          "type": "object",
          "required": ["state"],
          "properties": {
            "state": { "enum": ["valid", "questionable", "invalid"] },
            "flags": {
              "type": "array",
              "items": { "type": "string" }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "profile": { "type": "string", "description": "Optional profile/training route for simulator-only metrics." },
    "training_label": { "type": "string", "description": "Explicit label for simulator-derived training values that are not clinical measurements." },
    "artifact": { "type": "string", "description": "If the sample is artifact-laden, what kind, e.g. motion_possible." }
  }
}
