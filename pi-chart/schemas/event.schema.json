{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pi-rn.local/schemas/event.schema.json",
  "title": "pi-chart event envelope",
  "description": "One clinical claim/event envelope. Atomic structured events go in events.ndjson; narrative notes use frontmatter that conforms to note.schema.json and must have a matching communication event. Clinical types (observation/assessment/intent/action/communication/artifact_ref) require encounter_id, certainty, data, and links in addition to the base envelope. Structural types (subject/encounter/constraint_set) require only the base envelope.",
  "type": "object",
  "required": [
    "id",
    "type",
    "subject",
    "recorded_at",
    "author",
    "source",
    "status"
  ],
  "$defs": {
    "EvidenceRef": {
      "description": "Canonical EvidenceRef. Legacy structured refs remain accepted for v0.2 back-compat while parser normalization remains in place.",
      "oneOf": [
        {
          "type": "object",
          "required": ["ref", "kind"],
          "properties": {
            "ref": { "type": "string", "minLength": 1 },
            "kind": {
              "type": "string",
              "enum": ["event", "vitals_window", "note", "artifact", "external", "vitals"]
            },
            "role": {
              "type": "string",
              "enum": ["primary", "context", "counterevidence", "trigger", "confirmatory"]
            },
            "basis": { "type": "string", "maxLength": 500 },
            "selection": { "type": "object" },
            "derived_from": {
              "type": "array",
              "items": { "$ref": "#/$defs/EvidenceRef" }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["kind", "id"],
          "properties": {
            "kind": { "type": "string", "enum": ["event", "note", "artifact"] },
            "id": { "type": "string", "minLength": 1 }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["kind", "metric", "from", "to"],
          "properties": {
            "kind": { "type": "string", "enum": ["vitals"] },
            "metric": { "type": "string" },
            "from": { "type": "string", "format": "date-time" },
            "to": { "type": "string", "format": "date-time" },
            "encounterId": { "type": "string" }
          },
          "additionalProperties": false
        }
      ]
    }
  },
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable, unique identifier. Conventions (enforced by linting, not schema): evt_YYYYMMDDTHHMM[_suffix] for atomic events, note_YYYYMMDDTHHMM[_slug] for narrative notes. Structural entities (patient, encounter, constraint_set) use domain ids like 'patient_001', 'enc_001'."
    },
    "type": {
      "type": "string",
      "enum": [
        "observation",
        "assessment",
        "intent",
        "action",
        "communication",
        "artifact_ref",
        "encounter",
        "subject",
        "constraint_set"
      ],
      "description": "Closed set. The first six are clinical claim types per CLAIM-TYPES.md. The last three are chart-structural types used by patient.md, constraints.md, and encounter headers."
    },
    "profile": {
      "type": "string",
      "description": "Optional profile id; when set, must match an entry in schemas/profiles/index.json. See V-PROFILE-01."
    },
    "subtype": {
      "type": "string",
      "description": "Open-ended, snake_case. See CLAIM-TYPES.md for conventional values per type."
    },
    "subject": {
      "type": "string",
      "description": "Patient identifier. Must match chart.yaml subject."
    },
    "encounter_id": {
      "type": "string",
      "description": "Encounter this event belongs to. Required for clinical events; optional for subject/constraint_set."
    },
    "effective_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the claim was/is true in the world."
    },
    "effective_period": {
      "type": "object",
      "required": ["start"],
      "properties": {
        "start": {
          "type": "string",
          "format": "date-time"
        },
        "end": {
          "type": "string",
          "format": "date-time"
        }
      },
      "additionalProperties": false,
      "description": "Interval form for time-spanning events. Exactly one of effective_at or effective_period is required."
    },
    "recorded_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the claim was written to the chart."
    },
    "author": {
      "type": "object",
      "required": ["id", "role"],
      "properties": {
        "id": { "type": "string" },
        "role": { "type": "string" },
        "run_id": {
          "type": "string",
          "description": "For agent authors, the decision-cycle run identifier."
        }
      }
    },
    "source": {
      "type": "object",
      "required": ["kind"],
      "properties": {
        "kind": {
          "type": "string",
          "description": "Provenance channel. Canonical registry lives in DESIGN.md §1.1; schema stays permissive so validator severity can evolve independently of schema."
        },
        "ref": {
          "type": "string",
          "description": "Source-specific reference, e.g. a run_id or instrument id."
        }
      }
    },
    "transform": {
      "type": "object",
      "required": ["activity", "tool"],
      "properties": {
        "activity": {
          "type": "string",
          "enum": ["import", "normalize", "extract", "summarize", "infer", "transcribe"]
        },
        "tool": { "type": "string", "minLength": 1 },
        "version": { "type": "string" },
        "run_id": { "type": "string" },
        "input_refs": {
          "type": "array",
          "items": { "$ref": "#/$defs/EvidenceRef" }
        }
      },
      "additionalProperties": false,
      "description": "Activity-centric provenance. Declares the processing path that produced the payload. Distinct from source.kind (who) and author (which identifier)."
    },
    "certainty": {
      "type": "string",
      "enum": ["observed", "reported", "inferred", "planned", "performed"],
      "description": "Epistemic grade of the claim itself."
    },
    "status": {
      "type": "string",
      "enum": ["draft", "active", "final", "superseded", "entered_in_error"],
      "description": "Lifecycle of the claim within the chart."
    },
    "data": {
      "type": "object",
      "properties": {
        "status_detail": {
          "type": "string",
          "description": "Optional domain lifecycle detail. Allowed values are validator-enforced per (type, subtype); see CLAIM-TYPES.md and ADR 002."
        }
      },
      "description": "Type-specific payload. See CLAIM-TYPES.md for conventional shapes."
    },
    "links": {
      "type": "object",
      "properties": {
        "supports": {
          "type": "array",
          "items": {
            "oneOf": [
              {
                "type": "string",
                "minLength": 1,
                "description": "Bare string form. Either an event/note id (e.g. 'evt_20260418T0815_01' or 'note_20260418T0845_sbar') OR a vitals interval URI of the form 'vitals://<encounter_id>?name=<metric>&from=<iso8601>&to=<iso8601>[&unit=<unit>]'."
              },
              {
                "$ref": "#/$defs/EvidenceRef"
              }
            ]
          },
          "description": "Evidence supporting this claim. Mix of bare ids / vitals:// URIs (back-compat) and structured EvidenceRef objects (canonical for vitals windows and explicit note/artifact references). For assessments, validator rules still require at least one observation, vitals window, or artifact_ref among the supports."
        },
        "supersedes": {
          "type": "array",
          "items": { "type": "string" },
          "description": "IDs of events this claim replaces."
        },
        "corrects": {
          "type": "array",
          "items": { "type": "string" },
          "description": "IDs of events flagged as erroneous and replaced by this one."
        },
        "fulfills": {
          "type": "array",
          "items": { "type": "string" },
          "description": "IDs of intent events this action or outcome fulfills. Validator enforces that targets are type:'intent' within the same patient."
        },
        "addresses": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Problem-targeting targets only. IDs of assessment/problem events this claim addresses. Validator enforces target typing; loop-closing uses links.resolves in later phases."
        },
        "resolves": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Open-loop-closing or contradiction-resolution target ids. Validator enforces target typing per ADR 009 / V-RESOLVES-01."
        },
        "contradicts": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["ref", "basis"],
            "properties": {
              "ref": { "type": "string", "minLength": 1 },
              "basis": { "type": "string", "minLength": 1, "maxLength": 500 }
            },
            "additionalProperties": false
          },
          "description": "Structural disagreement with a prior peer claim. Direction and resolution semantics are enforced by ADR 009 validator rules."
        }
      }
    }
  },
  "oneOf": [
    {
      "required": ["effective_at"],
      "not": { "required": ["effective_period"] }
    },
    {
      "required": ["effective_period"],
      "not": { "required": ["effective_at"] }
    }
  ],
  "allOf": [
    {
      "if": {
        "properties": {
          "type": {
            "enum": [
              "observation",
              "assessment",
              "intent",
              "action",
              "communication",
              "artifact_ref"
            ]
          }
        },
        "required": ["type"]
      },
      "then": {
        "required": ["encounter_id", "certainty", "data", "links"]
      }
    }
  ]
}
