Build and verify the first implementation ultragoal after the clinical-truth-service seam planning run. This is a long-running, AFK-ready source-code ultragoal for a narrow `vital.sign` observation vertical slice spanning `pi-ledger` service-core semantics and `pi-chart` backend-mediated adapter tests. Respect all root/subproject boundaries: no hidden `pi-sim`, no direct browser/UI/EHR/`pi-agent` service writes, no TypeScript canonicalization/hash authority, no `pi-ledger` coupling to `pi-chart` brownfield internals, and no production transport/auth overreach.

- Baseline, archive, and AFK preflight: archive the completed prior ultragoal, capture dirty/untracked state and baseline verification, decide the first fact family as `vital.sign` observation, and write a run-state/unblocker artifact under `.scratch/pi-rn-clinical-truth-vital-sign-slice-ultragoal/`.
- Versioned registry support in Rust: add executable `ledger-core` support for a first-slice registry fixture/version/hash/content summary around `vital.sign` observation without making fixture policy a production ontology; prove duplicate/shape/object-field behavior and stable registry metadata.
- Rust-owned conformance vectors: add a generator and committed JSON vector suite for the `vital.sign` slice covering canonical bytes, Record hash, base append, revision append, point read, and required negative errors including patient mismatch, shape mismatch, non-canonical timestamps, caller-supplied K3 metadata, revision-not-allowed-for-append, stale/missing revision target, and idempotency conflict expectations.
- Clinical-truth service-core crate scaffold: add a private/transport-agnostic `pi-ledger` service-core crate around `ledger-core` with semantic request/response/error types for GetServiceInfo, GetRegistryInfo, ValidateClaim, and PreviewAppendAdmission, mapping kernel errors to the v1alpha1 vocabulary.
- Append/idempotency service behavior: implement AppendClaim, AppendRevisionClaim, and GetEntry in service-core with per-patient ordering, returned AcceptedEntryView data, deterministic `client_request_id` replay for the same payload, deterministic conflict for the same idempotency key with a different payload, and tests for retry/concurrency semantics without production transport.
- Point-read and snapshot/admin boundaries: implement PointRead, SnapshotLedger, and ValidateLedgerSnapshot service-core semantics with patient-scoped admin/maintenance context, cross-patient rejection, trusted rebuild behavior, and tests proving snapshot/rebuild is diagnostic only and not a write bypass.
- Per-patient WAL/log storage prototype: add a service-core storage seam and first file/WAL implementation for accepted entries with append record shape, fsync or explicit test-mode durability boundary, replay/head validation, corruption fail-closed behavior, and migration notes for later embedded stores behind the same contract.
- Pi-chart adapter contract tests before runtime adapter: add `pi-chart` tests/fixtures for VitalSample and EventEnvelope-to-Claim candidate mapping against the Rust-generated vector suite; assert patient/encounter/source/evidence/quality/time mapping, no hidden `pi-sim`, no direct service access, and no TypeScript canonicalization/hash computation.
- Pi-chart backend-mediated adapter slice: implement the minimal `pi-chart` clinical-truth adapter functions for the `vital.sign` vertical slice behind a backend/client seam and fake contract implementation; preserve chart write boundaries, suggestion/review boundaries, and clinician-surface projection from accepted fields rather than stored view truth.
- Cross-project conformance and boundary verification: wire a reproducible verification harness that regenerates/validates Rust vectors, runs service-core tests, runs `pi-chart` adapter tests, runs boundary scans for hidden sim/direct service imports, and records outputs in the run evidence directory.
- Documentation, ADR, and handoff updates: update relevant context/ADR/readme/scratch artifacts so the next agent knows what is implemented, what remains non-production, how to run verification, and which production transport/auth/registry decisions remain out of scope.
- Final quality gate: run full targeted verification, ai-slop-cleaner on changed files, rerun verification, run independent code-reviewer and architect review to APPROVE/CLEAR, fix non-clean findings, checkpoint with quality-gate JSON, and leave the next ultragoal unblocked.
